{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "## 1 - Commandes **Run** : `npm init -y `<br> **Run** : `npm install -D tailwindcss postcss-cli autoprefixer`<br> **Run** : `npx tailwindcss init -p`",
  "main": "index.js",
  "scripts": {
    "build:dev": "postcss -w src/tailwind.css -o public/style.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.1"
  }
}
